<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
<%= render 'layouts/abc', book: f.object %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
  <% end %>
    <%= link_to "Show",book_path %>
    <span> | </span>
	<%= link_to "Back",books_path %>

